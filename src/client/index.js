import React from 'react';
import ReactDOM from 'react-dom';
import MainContainer from './js/components/container/MainContainer';

const wrapper = document.getElementById('root');
ReactDOM.render(<MainContainer />, wrapper);
